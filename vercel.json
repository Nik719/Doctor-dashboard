{
      "buildCommand": "npm run build:client && npx esbuild api/index.ts --bundle --platform=node --target=node22 --outfile=api/index.js --external:mongoose --external:express --external:dotenv --external:dotenv/config --external:cors --format=esm",
      "outputDirectory": "dist/spa",
      "functions": {
              "api/index.js": { "maxDuration": 30 }
      },
      "routes": [
              { "src": "/api(.*)", "dest": "/api/index.js" },
              { "handle": "filesystem" },
              { "src": "/(.*)", "dest": "/index.html" }
      ]
}
